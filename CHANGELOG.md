# 更新日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)，日期格式 `YYYY-MM-DD`。

## [未发布]

### 素材生成侧直连视频生成平台（`ecom-video-creative`）

此前这个技能只到提示词为止——分镜、生成提示词、前三秒检查都写好了，但提示词要人自己粘到各家平台去出片。这一版把**「写好提示词 → 用户选模型 → 出投流视频」**这条链路接通：

- 新增 `scripts/providers.json`：预置 **12 个视频生成平台**的接入配置（火山方舟 Seedance / 豆包、阿里云百炼通义万相、可灵文生与图生、海螺 MiniMax、Google Veo、OpenAI Sora、Runway、Luma、Replicate、fal，外加不联网的离线演示 `mock`）。每个平台写清端点、鉴权方式、默认模型、比例写法、状态字段与取件方式；**加平台、换模型、改参数都只动这一个文件**，或用 `--provider-file` 传自定义覆盖配置。
- 新增 `scripts/video_render.py`：把分镜表的提示词按目标平台的参数名组装成请求，跑完**提交 → 轮询 → 取回成片 → 记生成台账**。`--list-providers` 列平台与所需环境变量，`--check` 只验凭据不生成，`--print-request` 打印实际请求体（密钥打码）。
- **用哪个大模型由用户定**：脚本不替用户选模型，只提供清单与默认值，`--provider` 选平台、`--model` 覆盖模型版本。
- **花钱的三道闸**：`--max-clips`（默认 10）硬上限、`--confirm` 显式确认（**不加就一个请求都不发**，只打印将要发出的请求体）、每笔提交与耗时全部进台账。生成按条计费，脚本不掌握各平台实时单价。
- **断点续跑**：已成功且成片还在的镜头默认跳过，不重复付费；`--overwrite` 才全部重生成。
- **首帧图**：`--first-frame-dir` 指定目录，按 `素材名_镜头号.png` → `素材名.png` 顺序找；本地图片自动转内联 data URI，可灵图生视频这类只要纯 base64 的会自动切出 base64（给的若是网络地址，先取回来再转码）；文件命名对不上时明确报出**缺哪个文件名**，不静默跳过。
- **生成台账**：20 列（素材名 / SKU / 市场 / 语种 / 镜头 / 时段 / 时长 / 平台 / 模型 / 比例 / 任务 ID / 状态 / 文件 / 本地路径 / 下载地址 / 提交时间 / 完成时间 / 耗时 / 重试 / 失败原因），`--out-xlsx` 出「生成台账 + 按状态分表」（失败行标红底），`--quarantine` 出未生成清单（带上当时的提示词，方便改完重投）。
- 文档：新增 [render-providers.md](skills/ecom-video-creative/references/render-providers.md)（逐平台凭据、注意事项、参数以官方文档为准的声明、花钱边界、常见坑）；`video-production.md` 增加「生成侧直连」一节，把工具链分工从「只交付提示词」升级为「可手动交付，也可直连生成」。

> 各平台参数名会随版本变化，端点是按 2026-09-14 的形状确认的；正式投产前用 `--check` 与 `--print-request` 对照官方文档核对一次，不对就改 `providers.json`。

### 相关

- 默认跑离线演示平台 `mock`：不联网、不花钱，只走通链路并产出与真跑完全一致的台账与产物格式，用来先看产物长什么样。
- README 与运行链路补上生成侧：技能清单、工作流分层表、`--out-xlsx` 工作表对照、示例演示脚本（`examples/demo.sh`）都加上「生成台账」，并说明**只有这条生成侧会联网，且默认不发出任何请求**。
- 顺手修掉两处可靠性问题：平台配置缺 `ok_values`（无法判断任务何时算完成）时直接拦下并提示补配置，而不是一直等到轮询超时；支持 `error_path`，把错误写在独立字段里的平台（如 Veo 的 `error`）失败即刻判定。

---

这一轮围绕两件事：**字迹潦草的单据怎么不失真地进账**，以及**门店实际的作业顺序**（先把纸质单据拍照进账，再拿平台 / ERP / 广告后台的表来核对）。

### 单据台账识别加固（`ecom-receipt-ledger`）

- 新增 [潦草手写加固手册](skills/ecom-receipt-ledger/references/handwriting-hardening.md)：字迹潦草的四种失效方式（脑补、串行、漏行、列错位）、四遍读法（骨架 → 逐行 → 交叉复核 → 价格库校准 → 生成补拍指令）、逐字段置信度、候选值机制、分组标记（圈号 / 打勾 / 画叉 / 跳号）读法、补拍指令模板、难点样例与验收标准。
- 识别提示词模板升级为两遍读法：第一遍只读骨架（行数、行号、方向、圈号、划掉的行），第二遍逐行抠四个关键格；输出增加 `skeleton` / `candidates` / `field_confidence` / `unreadable` / `self_check`，并明确禁止脑补、禁止把记号当数字、禁止跨行借数字、禁止把合计行当明细。
- 新增「**认不准不入账**」硬规则：字段带候选值（输入表 `候选值` 列）或置信度低于 `--confidence-floor`（默认 0.6）的行不进日结，进隔离清单与补拍清单；`--low-confidence`（默认 0.75）仍是告警线，入账但标黄并进异常行。
- 新增历史价格库核对 `--price-ref` / `--price-tolerance`（默认 ±30%）：单价分「命中区间 / 偏离区间 / 价格库未收录」三态，偏离只标不改（偏离也可能是行情变了）；价格库支持按「业务方向」分行写采购价与售价。
- 新增两类高频错位检出：数量与单价疑似写反（单价 ≤ 10 而数量 ≥ 100）、单价列疑似抄成金额（金额与单价同值且数量大于 1）。
- 新增两张工作表：**补拍清单**（按照片聚合，写清「哪一行、哪一格、怎么拍」，一次补拍解决一张单据上的所有未决项）与**价格核对**（偏离与未收录逐行列出，含建议动作）。
- 补拍清单**一行一个待办**（不再把同一张照片的多行挤进一个格子），拿着手机可以逐条对着拍、逐条销；未决行超过 5 行时「建议动作」自动改成「整张重拍」。
- 全部行都被隔离时不再提前退出：状态标 `blocked`（零笔入账），但明细 / 日结 / 补拍清单 / 隔离清单照样写出，并带上「识别到了哪些列」与隔离样例——正好是最需要补拍清单的时候，不能只丢一句「全部隔离」。
- 单据明细新增三列：`候选值` / `参考单价` / `价格核对`；关键格加底色——红底为金额不符、无法复核、字段有候选值，黄底为识别置信度偏低、单价偏离参考价。
- 示例数据补上埋点：新增「字段带候选值」与「新品类未映射」两类单据，并内置示例价格库 `examples/price_ref.csv`（含一条刻意偏离的参考价），演示脚本会真实跑出补拍清单与价格核对结果。
- 工作表由六张扩到八张：单据明细 / 日结 / 月结 / 异常行 / 待映射简写 / **补拍清单** / **价格核对** / 凭证索引（没有内容的表不写出）。

### 顺序与文档

- README 的痛点对照表把「手写单据拍照进账」调到「清单据」之前，并补一句「先单据、后清单据」：门店每天是先把纸质单据拍照进账，再拿平台 / ERP / 广告后台导出的表来核对；顺序反了最容易把采购记成销售、把数量和单价写反。
- [docs/run-chain.md](docs/run-chain.md) 与总控编排手册同步补上这条门店作业顺序，并在 ②B 卡片里加上加固识别与价格库两个输入。

### 表格写出器（`sheetio.py`，各技能同步）

- 新增**单元格底色**支持：工作表可传 `highlights`（`warn` 黄底 / `alert` 红底），需要标记的空值格也会写出底色；未使用该参数的技能输出不受影响。

## [v0.1.0] - 2026-09-14

首个公开版本：把跨境电商运营全链路拆成 **9 个可独立安装、可单独调用、可串成流水线**的 Agent 技能，每个技能自带业务手册、确定性核算脚本与风险兜底规则。装进支持 Agent Skills 约定的工具里就能直接用，运行依赖只有 Python 标准库。

覆盖 Amazon、Shopee、TikTok Shop、Temu、Lazada、Wayfair、美客多、独立站等平台；线下档口与门店的手写采购/销售单据，走 `ecom-receipt-ledger` 这条台账支线。

### 技能清单

| 阶段 | 技能 | 定位 | 主要产物 |
| --- | --- | --- | --- |
| 总控 | `crossborder-ecom-ops` | 总控编排 | 阶段路由、交接字段、卡点清单、SOP 与 Prompt 模板 |
| ① | `ecom-rules-fee` | 规则费率 | 合规费率对照表、逐单费用与净利明细、扣费风险清单 |
| ② | `ecom-data-prep` | 数据制表 | 干净结构化表、字段覆盖率、隔离行、清洗台账 |
| ②B | `ecom-receipt-ledger` | 手写单据台账 | 电子化采购/销售台账、日结与月结、算账差异清单、待映射简写清单、凭证索引（含照片缩略图） |
| ③ | `ecom-selection-profit` | 选品测算 | 逐行净利明细、站点汇总、保本价与目标售价 |
| ④ | `ecom-video-creative` | 视频素材 | 分镜脚本、文生视频提示词、前三秒留存检查、多语种语速预算 |
| ⑤ | `ecom-ads-plan` | 广告投放 | 投放结构表、出价预算表、保本 ROAS 与止损阈值 |
| ⑥ | `ecom-po-build` | PO 单制作 | PO 单、下单前校验报告、待人工确认清单 |
| ⑦ | `ecom-roi-review` | ROI 复盘 | 分组复盘表、环比变化、归因链与下周动作 |

### 全链路能力

- 串接顺序 `规则 → 数据 → 选品 → 素材 → 投流 → PO → 复盘`，复盘结论回流修正下一轮参数；① 与 ② 可并行，④ 与 ⑥ 可并行。②B 单据台账是独立支线，随时可跑，产出的标准品名台账与采销数据可喂给 ③ 补货、⑥ 下单、⑦ 核成本。
- 统一 JSON 输出信封（`task / status / confidence / data / flags / need_human_review / sources / assumptions / audit`），便于跨阶段串联、工单回写与事后追责。
- 四级风险分级与人工边界：涉及资金、退款、合规、平台设置的动作只输出判定与待办，落地永远由人执行。
- `ecom-data-prep` 支持中英双语长表头自动归一、ISO 8601 带时区日期、最多 6 位小数的账单明细金额精度。
- `ecom-video-creative` 内置 53 个语种的语速上限与 400+ 种语种写法，支持任意目标语种本地化；前三秒留存法则逐条检查钩子强度、首帧主体、开场禁忌、字幕长度与语速预算。

### 手写单据台账（`ecom-receipt-ledger`）

面向「纸质存根簿 + 手写单据」的门店与档口场景，把 `拍照 → 多模态识别 → 简写标准化 → 自动算账 → 采销日结 → 凭证回链` 做成一条闭环：

- **手写简写标准化**：归一化后精确匹配 → 最长子串兜底 → 未命中不猜，进「待映射简写」清单，避免把 `14pm改17pm` 这类简写猜错污染盘点与毛利统计。
- **自动算账与差异标红**：`数量 × 单价` 与单据手写金额逐笔比对，分「一致 / 小额差异 / 金额不符 / 金额缺失 / 无法复核」五档；金额不符与无法复核直接顶到 `high` 级 flag 与人工复核清单。方向写法自动归一（进货/入库/应付 → 采购，出货/应收 → 销售），判不出来的行走隔离清单而不是默认归类。
- **采购/销售日结**：按日期与月份归集采购金额、销售金额、净收益、净收益率、差异笔数、凭证数，输出日结对账报告。
- **凭证回链**：原始单据照片写成可点击超链接（本地 `file://` 或 `--photos-base` 云端地址），`--embed-photos` 时在「凭证索引」工作表内联缩略图；缩略图用系统图像工具生成（macOS `sips`，顺带把 iPhone 的 HEIC 转 JPEG），系统无该工具时退回「小图直嵌 / 大图跳过并记 flag」。
- 附带作业手册：`references/ocr-playbook.md`（拍照规范、六段式识别提示词、置信度分档、该停下来的信号）、`field-schema.md`（字段契约与三条硬约束）、`alias-dictionary.md`（起始简写词表与口径建议）、`daily-close-sop.md`（闭店 5 分钟对账 SOP）。

### 表格写出器（`sheetio.py`，各技能同步）

- 支持 `--out-xlsx` 一次产出多工作表 Excel 工作簿：表头加粗冻结首行、列宽自适应、数字写成数值可直接求和透视。
- 新增**单元格超链接**支持：`hyperlinks` 参数写出 `<hyperlinks>` 与 per-sheet rels，本地文件用 `file_href()` 转 `file:///` URL。
- 新增**图片嵌入**支持：`images` 参数按 oneCellAnchor 把图片锚进工作表（自动读 PNG/JPEG/GIF/BMP/WebP 尺寸并按框等比缩放），配套 `row_heights` 与 `column_widths` 控制版式。
- 字段别名扩充：单据场景的 `direction` / `doc_no` / `item_raw` / `item_std` / `amount_written` / `amount_calc` / `photo_path` / `confidence` 等，以及「交易日」「业务」「进价」「件数」等真实写法。
- 日期解析修复：`9.5` 这类「月.日」写法不再被误当作 Excel 序列号丢弃（配合 `default_year` 补年份）。
- 产物参数跨技能统一：`--out` 主表 CSV（UTF-8 BOM）、`--out-xlsx` Excel、`--out-md` Markdown 报告、`--quarantine` 问题行、`--out-json` 输出信封。
- 读表与报表写出均由内置 `sheetio.py` 用标准库完成，**不依赖 pandas、openpyxl**。

### 文档

- 新增 [docs/run-chain.md](docs/run-chain.md)「技能运行链路」：入口判断表（用户说什么进哪个技能）、链路全景图（主链路 + ②B 支线 + 复盘回流）、逐段运行卡片（触发 / 输入 / 命令 / 产物 / 交接字段 / 人工卡点）、三种真实跑法（新品上线、门店闭店、周度复盘）、安装建议与跨阶段两条硬规则。
- 新增 [docs/social-preview.png](docs/social-preview.png)：1280×640 仓库社交预览图（八阶段流水线示意）。
- 新增 [docs/release-notes-v0.1.0.md](docs/release-notes-v0.1.0.md)：本版 Release 说明。
- README 新增「这套技能解决什么问题」对照表，用运营视角说明六个高频痛点与对应做法；补充阶段 2B 与台账支线说明、Excel 超链接与图片嵌入能力、示例埋点清单、运行链路入口，仓库结构补上 `docs/` 与 `CHANGELOG.md`。
- `.github/ABOUT.md` 提供 About 文案与 Topics 建议：占满 20 个标签，覆盖品类词、平台词、场景词（含手写单据 / 拍照记账 / OCR）与技术生态词；另含「技能运行链路」一段话与仓库文件索引。
- 总控技能内置 ②B 路由与「录入类」任务分类；编排手册定义 ②B → ③/⑥/⑦ 的交接字段。

### 工程约定

- 只用 Python 标准库，Python 3.8+ 可跑，无第三方依赖。
- 一个技能 = 一个自包含文件夹，可单独复制走，内部相对路径不依赖仓库根目录。
- 配置与阈值外置（费率表、字段别名、告警阈值走数据文件或命令行参数），不写死在提示词里。
- 每个技能自带示例数据与 `demo.sh`，示例里刻意埋了脏数据，异常检测路径会真实触发。

### 已知边界

- 费率与政策数据需要按 `as_of` 由人工定期更新，技能只负责结构化与核算，不负责联网抓取。
- 不直连平台后台接口，输入来自后台导出、ERP 或人工维护表。
- 脚本只做确定性计算与校验；归因结论、谈判话术、投放策略等判断性内容由 Agent 结合手册生成，需人工复核。

[v0.1.0]: https://github.com/xianglouw/ecom-agent-skills/releases/tag/v0.1.0
